import React from 'react';

export default React.createClass({
  render() {
    return (
      <div>
        <h2>About</h2>
        <p>A starting place for a simplified DuraCloud UI using React JS, Browserify, Gulp and ES6.</p>
      </div>
    );
  }
});
